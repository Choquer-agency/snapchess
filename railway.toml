[build]
builder = "nixpacks"
buildCommand = "npm ci && npm run build -w packages/shared && npx prisma generate --schema=apps/api/prisma/schema.prisma && npm run build -w apps/api"

[deploy]
startCommand = "node apps/api/dist/index.js"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
